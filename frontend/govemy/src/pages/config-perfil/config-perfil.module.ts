import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ConfigPerfilPage } from './config-perfil';

@NgModule({
  declarations: [
    ConfigPerfilPage,
  ],
  imports: [
    IonicPageModule.forChild(ConfigPerfilPage),
  ],
})

export class ConfigPerfilPageModule {}
